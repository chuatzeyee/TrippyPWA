# Trippy

AI-powered travel planning PWA. Plan a trip through a 7-step wizard, then get a full day-by-day itinerary generated by Gemini 2.5 Flash with real venues, transport directions, and budget breakdowns.

Built with vanilla JS, Vite, and Supabase. No frontend framework.

**Live:** Deployed via GitHub Pages with CI/CD.

---

## Quick Start

```bash
npm install
cp .env.example .env.local
# Fill in your Supabase URL and anon key
npm run dev
```

Open `http://localhost:5173`.

### Environment Variables

| Variable | Where | Purpose |
|----------|-------|---------|
| `VITE_SUPABASE_URL` | `.env.local` | Supabase project URL |
| `VITE_SUPABASE_ANON_KEY` | `.env.local` | Supabase anonymous/public key |
| `GEMINI_API_KEY` | Supabase secrets | Gemini 2.5 Flash API key (edge function) |
| `GOOGLE_PLACES_API_KEY` | Supabase secrets | Google Places API (New) key for venue photos |

---

## Tech Stack

| Layer | Choice | Why |
|-------|--------|-----|
| Frontend | Vanilla JS (ES modules) | Zero framework overhead, small bundle |
| Bundler | Vite 8 | Fast HMR, native ESM dev server |
| Styling | CSS custom properties | Design token system, no preprocessor |
| Backend | Supabase | Postgres, Auth, Edge Functions, Row Level Security |
| Auth | Supabase Auth (Google OAuth) | Social login, session management |
| AI | Gemini 2.5 Flash | Itinerary generation via structured JSON output |
| Photos | Google Places API (New) | Venue photos by place ID or text search |
| Routing | Hash-based SPA | `#/wizard/3`, `#/trip/:id` - works on static hosts, auth callback guard |
| Fonts | DM Serif Display + Figtree + JetBrains Mono | Editorial serif + clean sans + monospace data |
| Deploy | GitHub Pages + GitHub Actions | Auto-deploy on push to main |

---

## Project Structure

```
dev/
  index.html                          Entry point, font loading, PWA meta tags
  vite.config.js                      Vite config (GH_PAGES base path support)
  public/
    manifest.json                     PWA manifest
    icons/
      favicon.svg                     App icon (SVG source)
      icon-192.png                    PWA icon 192x192
      icon-512.png                    PWA icon 512x512
  css/
    tokens.css                        Design tokens (colors, spacing, typography, shadows)
    base.css                          Reset, utilities, buttons, inputs, chips, cards
    components/
      dashboard.css                   Landing page, trip cards, floating particles
      wizard.css                      All wizard step styles
      trip-detail.css                 Trip itinerary view (days, activities, transport)
      modal.css                       Modal dialog
      nav.css                         Top navigation bar
      auth.css                        Auth gate modal
      profile-wizard.css              Profile setup flow
  js/
    main.js                           App bootstrap, route definitions, auth guard
    router.js                         Hash-based SPA router with parameterized routes
    auth/
      auth.js                         Supabase Auth wrapper, session management
      auth-ui.js                      Auth gate modal (Google OAuth)
    components/
      dashboard.js                    Landing page, trip cards, empty state, floating particles
      nav.js                          Top navigation bar with auth state
      trip-detail.js                  Full itinerary view - days, activities, transport, photos
    data/
      currencies.js                   Static exchange rates (~35 currencies), convert(), format()
      transit-lines.js                Transit line abbreviations for 20+ cities worldwide
      user-prefs.js                   localStorage: home currency, onboarding flag
      day-builder.js                  Day planning utilities, escapeHtml()
      registry.js                     Trip storage and retrieval helpers
      trip-repository.js              Supabase trip CRUD operations
      profile-repository.js           Supabase profile fetch/update, setup check
    lib/
      supabase.js                     Supabase client initialization
      locale.js                       Locale detection from user flag
    services/
      generate.js                     Generate itinerary API call
      generation-manager.js           Generation state machine (polling, retry, status)
    wizard/
      wizard.js                       Wizard shell, all 8 step renderers, flag backgrounds
      wizard-state.js                 sessionStorage state, validation, immutable updates
      calendar.js                     Date range picker (month grid, click-to-select)
      destinations.js                 124 destinations with photos, search, popular list
    profile/
      profile-wizard.js               First-time profile setup (name, home city, currency)
  supabase/
    functions/
      generate-itinerary/index.ts     Gemini 2.5 Flash edge function - prompt + JSON schema
      places-photo/index.ts           Google Places API (New) photo proxy
    migrations/
      001_initial_schema.sql          Profiles, trips, itinerary_days, activities + RLS
      002_transport_options.sql       Transport options JSONB column on activities
      003_day_weather.sql             Weather JSONB on itinerary_days
      004_trip_extras.sql             Trip subtitle, emoji, cover_image columns
      005_profile_location.sql        Home city, country, flag on profiles
      006_add_generating_status.sql   "generating" status for trips
  .github/
    workflows/
      deploy.yml                      GitHub Actions: build + deploy to GitHub Pages
  .interface-design/
    system.md                         Full design system documentation
```

---

## Features

### Trip Planning Wizard

A 7-step wizard that collects trip preferences and feeds them to the AI:

| Step | Title | What it collects |
|------|-------|-----------------|
| 1 | Where to? | Single destination or multi-city route (124 cities, search + popular grid) |
| 2 | When's the adventure? | Traveler count, exact dates (calendar picker) or flexible duration/season |
| 3 | Comfort level | Budget preset (backpacker/comfortable/luxury) + daily amount in destination currency |
| 4 | Where will you stay? | Accommodation type, hotel star rating, priority chips. "Already Settled" toggle to skip if pre-booked |
| 5 | How do you fly? | Fare class, departure time prefs, connection preference. "Already Settled" toggle to skip if pre-booked |
| 6 | How do you travel? | Style sliders (nightlife, pace, food, exploration) + interest chips (max 8) |
| 7 | Trip summary | Grid overview of all choices + collapsible extras (must-do, dietary, avoid) |

**Smart transport detection:** For nearby destinations (e.g. Singapore to Batam), the wizard skips the flights step and asks for ferry/train/bus preference instead.

### AI Itinerary Generation

The `generate-itinerary` edge function sends wizard state to Gemini 2.5 Flash with a detailed prompt and JSON schema. The AI returns:

- **Day-by-day itinerary** with hour-by-hour activities, real venue names, addresses, coordinates
- **Transport options** for every activity - up to 3 alternatives (walk, public transit, ride-share) with boarding/alighting stations, duration, cost
- **Flight or transport suggestions** - airlines + flight numbers for air travel, operators + terminals for ferry/bus/train
- **Accommodation options** - 2-3 hotels at different price points with badges (Recommended, Best Value, etc.)
- **Booking checklist** - grouped into "Must Book Ahead" and "Good to Book" with day numbers
- **Weather forecasts** per day based on destination and season

### Trip Detail View

Once generated, each trip gets a full itinerary view with:

- Day navigation with weather icons and temperature
- Activity cards showing time, venue, description, tips, cost, and venue photos
- Multi-leg transport parsing - handles complex directions like "Walk to Fort Canning MRT, take DTL to Stevens, then change to TEL to Orchard Boulevard"
- Transit line abbreviation system covering 20+ major cities (Singapore, Tokyo, London, NYC, etc.)
- Getting-there cards with step-by-step transport legs and visual connectors
- **Adaptive transport cards** - card title, icon, and booking links morph based on travel mode: "Ferry" (Rome2Rio), "Bus" (Rome2Rio), "Rail" (Rome2Rio), or "Getting There" (Google Maps). Flight trips show "Flights" with Google Flights links
- Accommodation recommendations with booking links (Booking.com, Agoda, Google Hotels)
- Booking checklist with priority grouping
- **Live trip tracking** - pulsating teal timeline knob on today's activities, "Current Activity" button on dashboard cards
- Floating back pill in standalone PWA mode (replaces hidden nav bar)

### Authentication & Security

- Google OAuth via Supabase Auth (implicit grant flow)
- Auth gate modal for protected routes (wizard, profile)
- Auto-create profile on first sign-in
- Profile wizard for first-time setup (display name, home city/country, currency)
- **3-layer OAuth token defense:** router skips auth callback hashes, 404 page never renders raw paths, hash cleaned after auth initialization
- XSS-safe: all user-controlled strings passed through `escapeHtml()` before `innerHTML` injection
- Console warnings suppressed in production builds (DEV-only logging)

### Dashboard

- **Authenticated:** Trip cards grid with destination emoji, title, date range, status badge, "Current Activity" button on active trips
- **Unauthenticated:** Animated landing page with split-flap airport display animation, city thumbnail shortcuts

---

## Database Schema

Six migrations build up the Postgres schema. All tables use Row Level Security - users can only access their own data.

| Table | Purpose |
|-------|---------|
| `profiles` | User profile (display name, avatar, home currency, home city/country) |
| `trips` | Trip metadata (title, dates, budget, status, wizard state as JSONB) |
| `itinerary_days` | Per-day data (title, theme, weather JSONB) |
| `activities` | Individual activities (venue, time, cost, coordinates, transport options JSONB) |

Trip statuses: `planning` -> `generating` -> `generated` -> `active` -> `completed`

Auto-profile creation trigger on `auth.users` insert populates name and avatar from OAuth metadata.

---

## Design System

**Dark Lounge** - warm charcoal base with warmth coming from colored surfaces, not the background. Premium travel magazine read at night.

| Token | Value | Role |
|-------|-------|------|
| `--parchment` | `#1B1A17` | Warm charcoal canvas |
| `--surface-solid` | `#252320` | Card backgrounds |
| `--ink` | `#E5E1D8` | Primary text (warm cream) |
| `--terracotta` | `#D47358` | Primary accent - CTA, selections, active states |
| `--teal` | `#4A9684` | Secondary accent - links, today marker |
| `--amber` | `#D09A48` | Data emphasis, star ratings |
| `--sage` | `#5FA072` | Progress done state |

Typography: DM Serif Display for headings (editorial feel), Figtree for body text, JetBrains Mono for costs/times/data.

Full design system including component patterns, depth strategy, and spacing scale documented in `.interface-design/system.md`.

---

## Destinations

124 cities across 45+ countries. Each destination record includes:

- Name, country, emoji flag
- Lat/lng coordinates
- Local currency code + symbol
- Timezone
- Budget ranges (backpacker/comfortable/luxury in USD/day)
- Wikipedia thumbnail URL

Regions covered: Japan, Australia, Spain, Indonesia, France, USA, Thailand, UK, South Korea, Italy, Turkey, Portugal, Taiwan, Singapore, Netherlands, UAE, Czech Republic, Vietnam, Iceland, Morocco, South Africa, Malaysia, Argentina, Mexico, Greece, Austria, Hungary, Germany, Denmark, Sweden, Switzerland, Ireland, Cuba, Peru, Colombia, Philippines, New Zealand, Qatar, Oman, Jordan, Saudi Arabia, Canada, Croatia, Poland, Belgium.

Includes SE Asia holiday spots popular with Singaporean travelers: Genting Highlands, Cameron Highlands, Batam, Bintan, Desaru, Krabi, Koh Samui, Da Nang.

---

## Transit Line Abbreviations

The `transit-lines.js` module maps long transit line names to short abbreviations for display in transport pills. Covers ~100 lines across 20+ cities:

- **Singapore:** MRT lines (NSL, EWL, NEL, CCL, DTL, TEL), LRT
- **Hong Kong:** MTR lines (TWL, ISL, KTL, TKL, TCL, AEL, EAL, SIL)
- **Tokyo:** Yamanote, Chuo, Marunouchi, Ginza, Hibiya, Tozai, etc.
- **London:** Tube lines (Central, Victoria, Northern, Jubilee, etc.)
- **Paris:** RER A-E
- **NYC:** Subway lines by color/number
- **Plus:** Seoul, Taipei, Bangkok, Kuala Lumpur, Jakarta, Berlin, Amsterdam, SF BART, Chicago L, Toronto TTC, Dubai Metro, Sydney Metro

Parenthetical abbreviations are extracted automatically (e.g. "Thomson-East Coast Line (TEL)" becomes "TEL"). Names over 18 characters that match the lookup table get shortened.

---

## Edge Functions

### `generate-itinerary`

Accepts wizard state JSON, constructs a detailed Gemini prompt with trip parameters (destination, dates, budget, pace, interests, accommodation, transport mode), and returns structured itinerary JSON. Uses Gemini 2.5 Flash with `responseMimeType: "application/json"` and a comprehensive response schema.

Key prompt features:
- Activity count scales with pace slider (5-6 for relaxed, 11-14 for packed)
- Same-city detection skips flight/arrival logistics for local exploration trips
- Nearby-trip detection replaces flights with ferry/bus/train options
- Free/work days get light schedules (breakfast, lunch, dinner only)
- Region-appropriate ride-share apps (Grab for SE Asia, Gojek for Indonesia, etc.)

### `places-photo`

Proxies Google Places API (New) photo requests. Accepts either `place_id` (direct lookup) or `query` + optional `location` (text search). Returns a signed photo URL. Responses are cached for 24 hours.

---

## Deployment

### GitHub Pages (Production)

Push to `main` triggers the GitHub Actions workflow:

1. `npm ci` + `npx vite build` with `GH_PAGES=1` (sets base path to `/TrippyPWA/`)
2. Supabase URL and anon key injected from GitHub Secrets
3. Built artifacts uploaded and deployed to GitHub Pages

### Supabase Setup

1. Create a Supabase project
2. Run migrations in order via SQL Editor or `supabase db push`
3. Enable Google OAuth in Authentication > Providers
4. Set edge function secrets:
   ```bash
   supabase secrets set GEMINI_API_KEY=your-key
   supabase secrets set GOOGLE_PLACES_API_KEY=your-key
   ```
5. Deploy edge functions:
   ```bash
   supabase functions deploy generate-itinerary
   supabase functions deploy places-photo
   ```

### Local Development

```bash
npm install
npm run dev       # Vite dev server on :5173
npm run build     # Production build to dist/
npm run preview   # Preview production build
```

---

## State Management

| Store | Key | Purpose |
|-------|-----|---------|
| `sessionStorage` | `trippy_wizard_state` | Wizard state - survives page refresh, clears on tab close |
| `localStorage` | `trippy_user_prefs` | Home currency and symbol - persists across sessions |
| Supabase | `profiles` table | User profile synced on login |
| Supabase | `trips` + related tables | All trip data, itineraries, activities |

All state updates use immutable spread patterns. Wizard state is validated per-step with `canAdvance()` before allowing forward navigation.

---

## Currency System

Static exchange rates for ~35 currencies in `currencies.js`. All rates are USD-based.

- `convert(amount, fromCode, toCode)` - cross-currency conversion via USD pivot
- `formatCurrency(amount, symbol)` - locale-formatted display string

Budget display logic: preset amounts are in USD/day, converted to destination currency for the stepper, with home currency equivalent shown as a subtitle when different.

---

## PWA

Installable as a Progressive Web App:

- `manifest.json` with app name, icons (192px, 512px), theme color (`#1B1A17`)
- Viewport configured for mobile with `viewport-fit=cover`
- Apple mobile web app meta tags for iOS home screen
- Back-to-top floating button appears after 400px scroll, positioned above the sticky footer
- Nav bar hidden in standalone PWA mode on mobile, replaced with floating back pill
- Wizard CTA pinned to glass-morphism fixed footer bar
